# ninja-publisher .env — Phase E deployment
# Copy to /home/uniceadmin/ninja-clan/agent-teams/worktrees/ninja-publisher/.env
# chmod 600 after populating with real values
# DO NOT commit with real values
#
# FORMAT RULES (systemd EnvironmentFile is strict):
#   CORRECT:   KEY=value
#   WRONG:     export KEY=value     (no 'export' prefix)
#   WRONG:     KEY="value"          (quotes become part of value)
#   WRONG:     KEY=value # comment  (comment becomes part of value)
#   WRONG:     KEY = value           (no spaces around =)

# Medium (Playwright browser automation -- no API token needed)
# MEDIUM_SESSION_PATH=~/.config/ninja-publisher/medium-session.json
# MEDIUM_PUBLISH_TIMEOUT_SECONDS=60
MINIMAX_API_KEY=
NOTION_TOKEN=
NOTION_DB_ID=eda57771e01e461f8259e218259fb42d
SLACK_BOT_TOKEN=
SLACK_SIGNING_SECRET=
SLACK_CHANNEL_ID=C0ASL49F2P8
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
CANONICAL_BASE_URL=https://unicebondoc.com/blog
